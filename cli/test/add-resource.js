const { expect}  = require("chai");
// const { expect } = chai.expect;

describe("add-resource", () => {
  it("should fail if resource name were not given", () => {
    expect(null).to.be.eql(false);
  });
});
